import {
  Meta,
  Canvas,
  Story,
  ArgsTable,
  Description,
} from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';
import { action } from '@storybook/addon-actions';
import { WCIconComponent } from './goa-icon.component';
import { ExperimentalComponentsModule } from '../components.module.ts';
import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import 'goa-web-components';

<Meta
  title="Experimental/Components/WCIcon"
  component={WCIconComponent}
  decorators={[
    moduleMetadata({
      imports: [ExperimentalComponentsModule],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    }),
  ]}
  argTypes={{}}
/>

export const Template = (args) => {
  return {
    template: `
    <goa-ng-icon [type]="type" [size]="size"></goa-ng-icon>`,
    props: args,
  };
};

<Story
  name="Basic"
  args={{
    type: "alarm",
    size: "large",
  }}
>
  {Template.bind({})}
</Story>

