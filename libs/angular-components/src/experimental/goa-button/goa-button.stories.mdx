import {
  Meta,
  Canvas,
  Story,
  ArgsTable,
  Description,
} from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';
import { action } from '@storybook/addon-actions';
import { WCButtonComponent } from './goa-button.component';
import { ExperimentalComponentsModule } from '../components.module.ts';
import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import 'goa-web-components';

<Meta
  title="Experimental/Components/WCButton"
  component={WCButtonComponent }
  decorators={[
    moduleMetadata({
      imports: [ExperimentalComponentsModule],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    }),
  ]}
  argTypes={{}}
/>

export const Template = (args) => {
  return {
    template: `<goa-ng-button text='test' size="medium" type="primary" > </goa-ng-button>`,
    props: args,
  };
};

<Story
  name="Basic"
  args={{
    text: "Test",
    size: "medium",
    type: "primary"
  }}
>
  {Template.bind({})}
</Story>