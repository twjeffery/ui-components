
import {
  Meta,
  Canvas,
  Story,
  ArgsTable,
  Description,
} from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';
import { action } from '@storybook/addon-actions';
import { WCInputComponent } from './goa-input.component';
import { ExperimentalComponentsModule } from '../components.module.ts';
import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import 'goa-web-components';

<Meta
  title="Experimental/Components/WC Input"
  component={WCInputComponent}
  decorators={[
    moduleMetadata({
      imports: [ExperimentalComponentsModule],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    }),
  ]}
  argTypes={{}}
/>

export const Template = (args) => {
  return {
    template: `
   <goa-ng-input [id]="id" [name]="name" [value]="value"  [trailingicon]="trailingicon" [handletrailingiconclick]="handletrailingiconclick" > </goa-ng-input>`,
    props: args,
  };
};

<Story
  name="Basic"
  args={{
    id: "testId",
    name: "testName",
    handletrailingiconclick: true,
    trailingicon: "open",
    value: "Yabba Dabba do"
  }}
>
  {Template.bind({})}
</Story>
