
import {
  Meta,
  Canvas,
  Story,
  ArgsTable,
  Description,
} from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';
import { action } from '@storybook/addon-actions';
import { WCInputComponent } from './goa-input.component';
import { ExperimentalComponentsModule } from '../components.module.ts';
import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import 'goa-web-components';

<Meta
  title="Experimental/Components/WC Input"
  component={WCInputComponent}
  decorators={[
    moduleMetadata({
      imports: [ExperimentalComponentsModule],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    }),
  ]}
  argTypes={{value: {
      control: 'string',
      table: {
        //tracked bug in storybook https://github.com/storybookjs/storybook/issues/11983, need table to override display
        defaultValue: { summary: "test" },
        type: { summary: 'string' },
      },
    }}}
/>

export const Template = (args) => {
  return {
    template: `
   <goa-ng-input [id]="id" [name]="name" [trailingicon]="trailingicon" [handletrailingiconclick]="handletrailingiconclick" [value]="value"> </goa-ng-input>`,
    props: args,
  };
};

<Story
  name="Basic"
  args={{
    id: "testId",
    value: "Yabba Dabba do",
    name: "testName",
    handletrailingiconclick: true,
    trailingicon: "open",
  }}
>
  {Template.bind({})}
</Story>
