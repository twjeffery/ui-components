import {
  Meta,
  Canvas,
  Story,
  ArgsTable,
  Description,
} from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';
import { action } from '@storybook/addon-actions';
import { WCBadgeComponent } from './goa-badge.component';
import { ExperimentalComponentsModule } from '../components.module.ts';
import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import 'goa-web-components';

<Meta
  title="Experimental/Components/WC Badge"
  component={WCBadgeComponent}
  decorators={[
    moduleMetadata({
      imports: [ExperimentalComponentsModule],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    }),
  ]}
  argTypes={{}}
/>

export const Template = (args) => {
  return {
    template: `
    <goa-ng-badge [content]="content"  [type]="type" [icon]="icon" [testId]="testId"></goa-ng-badge>
    `,
    props: args,
  };
};

<Story
  name="Success"
  args={{
    content: "Success",
    type: "success",
    icon: false,
    testId: "badge-test"
  }}
>
  {Template.bind({})}
</Story>

